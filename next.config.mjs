/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'assets.aceternity.com',
      'images.unsplash.com',
      'lh3.googleusercontent.com',
      'lh3.google.com',
      'i.imgur.com' // Imgur domain added here
    ],
  },
};

export default nextConfig;
